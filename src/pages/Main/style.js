import styled from 'styled-components';

export const Position = styled.div`
  width: 1400px;
  overflow: scroll;
  &::-webkit-scrollbar {
    display: none;
  }
  margin: 0 auto;
  display: flex;
  flex-wrap: wrap;
`;
