import { props } from 'react';
import styled, { css } from 'styled-components';

export const TextBox = styled.textarea`
  width: 500px;
  height: 300px;
  resize: none;
  outline: none;
  font-size: 24px;
`;
